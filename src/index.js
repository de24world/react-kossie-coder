import { StrictMode } from "react";
import ReactDOM from "react-dom";

import App from "./15/App";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <StrictMode>
    <App />
  </StrictMode>,
  rootElement
);
